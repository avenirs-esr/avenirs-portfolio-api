name: Main Workflow

on:
  pull_request:
    branches: [main]

jobs:
  call-main-ci:
    uses: avenirs-esr/avenirs-workflows/workflows/common-backend-worflow/workflow.yaml@feat/core-actions-workflows
